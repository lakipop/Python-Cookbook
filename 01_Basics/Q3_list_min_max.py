# 3. Find Max and Min in a list of 10 numbers
numbers = [15, 82, 33, 4, 99, 21, 8, 45, 67, 12]

print(f"List of numbers: {numbers}")

# Using built-in functions
max_val = max(numbers)
min_val = min(numbers)

print(f"Maximum value: {max_val}")
print(f"Minimum value: {min_val}")
