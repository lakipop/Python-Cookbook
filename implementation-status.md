# Implementation Status - Python-Cookbook

## Completed Practical Work
- **Practical 01: Introduction to Python Programming** (`practical_01.py`)
  - Set up one interactive file with cells for all tasks.
  - Completed Tasks 1-5 (basic syntax, variables, datatypes, string manipulation).
  - Completed Additional Exercises 1-19 covering practical math formulations and string methods. 

- **Practical 02: Python Booleans, Operators, Type Conversion, and Lists** (`practical_02.py`)
  - Covered booleans, logical conditions, input type casting, and Python list implementations natively via `# %%`.
  - Implemented 14 baseline tasks and 6 additional exercises covering sets, max/min, input gathering, and average computations. 

- **Practical 03: Python Tuples, Sets and Dictionaries** (`practical_03.py`)
  - Represented Tuples (immutability logic), Sets (unique elements, operations), and Dictionaries (key-value maps).
  - Created 27 implementation blocks plus 20 specialized activities natively mapping dictionary merges, frozen sets, tuple slicing, and dict sorting logic.

- **Practical 04: Flow Controls and Functions** (`practical_04.py`)
  - Covered loop branching (`if-elif-else`), variable scopes, and iterative boundaries (`range`, `while`).
  - Implemented default loop terminators (`break`, `continue`, `pass`).
  - Structured reusable components using positional & keyword arguments (`**kwargs`) and functional recursions within `practical_04.py`.

- **Practical 05: File Handling, Modules, Exceptions & OOP** (`practical_05.py`, `mymath.py`, `calculator.py`)
  - Handled File I/O explicitly writing, appending, and reading buffers via `with open(...)`. Trapping IO exceptions locally via `try/except`.
  - Built physical Python module extensions (`mymath.py`, `calculator.py`) interacting with the central script safely mimicking real-world architectural splitting. 
  - Overhauled Object-Oriented layouts defining Classes (`__init__`), tracking Encapsulation state logic (`self.__balance`), deriving Inheritance, and demonstrating object Polymorphism recursively.

- **Practical 06 Part 1: Search Strategies and Logic** (`practical_06_part1.py`)
  - Analyzed Uninformed Search mapping logic.
  - Built `collections.deque` implementations configuring Breadth-First Searches for shortest-path graph routes and complex 2D Maze traversals.
  - Deployed Depth-First Searches (DFS) tracking internal LIFO stacks dynamically preventing cyclic loops. Mapped comparative DFS/BFS analysis engines.
